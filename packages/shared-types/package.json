{
  "name": "@web3-gaming/shared-types",
  "version": "1.0.0",
  "description": "Shared TypeScript types for web3 gaming platform",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "prepublish": "npm run clean && npm run build"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}